# Source Layout

`core` owns infrastructure, `design-system` owns UI, `features` owns product domains, and `shared` owns generic helpers.
Keep route files thin and compose feature components from `app`.
